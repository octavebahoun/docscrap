# Variables d'environnement pour le développement local
# Le proxy Vite est utilisé, donc pas besoin de VITE_API_URL

# Si vous voulez pointer directement vers le backend sans proxy:
# VITE_API_URL=http://localhost:3000
