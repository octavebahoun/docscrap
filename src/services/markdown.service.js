/**
 * Service de conversion HTML → Markdown
 * À implémenter ultérieurement
 */

function convertToMarkdown(htmlContent) {
  // TODO: Implémenter la conversion HTML → Markdown
  // Utiliser une bibliothèque comme html-to-markdown ou turndown
  console.warn('convertToMarkdown: À implémenter');
  return '';
}

module.exports = {
  convertToMarkdown
};
